from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A small mlops project showing use of DVC, mlflow, model registry in an ML project',
    author='Abhigyan',
    license='',
)
